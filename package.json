{
    "name": "test-driven-development",
    "version": "1.0.0",
    "description": "write test suits for a function that takes an array of numbers, determine which numbers are even or odd, then return an object of an array of odd and even numbers respectively.",
    "main": "test.js",
    "scripts": {
        "test": "nyc --reporter=html --reporter=text mocha --timeout 500000 --compilers js:babel-core/register tests/test.js"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "chai": "^4.1.2",
        "mocha": "^4.0.1"
    },
    "devDependencies": {
        "babel": "^6.23.0",
        "babel-cli": "^6.26.0",
        "babel-core": "^6.26.0",
        "babel-preset-env": "^1.6.1",
        "babel-register": "^6.26.0",
        "mocha-lcov-reporter": "^1.3.0",
        "nyc": "^11.3.0"
    }
}